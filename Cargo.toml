[package]
name = "openrarust"
version = "0.1.0"
edition = "2021"
description = "RTS-grunnmur i OpenRA-ånd som kjorer i nettleseren med WebGL (Rust + WASM)"

[dependencies]
macroquad = "0.4"

[profile.release]
opt-level = 3
lto = true
codegen-units = 1
panic = "abort"
